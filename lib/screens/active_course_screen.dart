import 'package:flutter/cupertino.dart';

class ActiveCourseScreen extends StatelessWidget {
  const ActiveCourseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Active Course Screen');
  }
}
